<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lamorena">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_car"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_icon"
        android:supportsRtl="true"
        android:theme="@style/AppThemeMaterial">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity
            android:name=".Activities.ProfileActivity"
            android:label="@string/title_activity_product_info"
            android:theme="@style/AppThemeMaterial.NoActionBar"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".Activities.RegistryUser"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".Activities.Employee"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".Activities.Service"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".Activities.Insurer"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.registrarEmpleado"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.updateEmpleado"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.registrarServicio"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.updateServicio"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.registrarInsurer"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.updateInsurer"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.listEmpleado"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".Activities.IngresoCliente"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".Activities.IngresoVehiculo"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.registrarIngreso"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.listIngresos"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.listAseguradora"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".crud.listServicio"
            android:theme="@style/AppThemeMaterial" />
        <activity
            android:name=".Activities.Login"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppThemeMaterial.NoActionBar" />

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name" />
    </application>

</manifest>